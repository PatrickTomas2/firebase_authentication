import 'package:flutter/material.dart';

class HomeLabwork extends StatelessWidget {
  const HomeLabwork({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: const Text('This is the home'),
      ),
    );
  }
}
